package basic;

public class VolumeOfSphere {
    static void main() {
        double v;
        double r=5;
        v = (4.0 / 3.0) * 3.14 * r * r * r;
        System.out.println("Volume: " + v);
    }
}
